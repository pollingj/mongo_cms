<% simple_form_for [:admin, @content_type] do |f| %>
	<fieldset>
		<legend>Content Type</legend>
  		<%= f.input :title %>
		<%= f.button :submit, "#{@content_type.title == nil ? "Create" : "Edit"} content type" %>
	</fieldset>
<% end %>